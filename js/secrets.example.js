/* Khoá bí mật – file này KHÔNG đưa lên git (.gitignore). Khi deploy được sinh tự động từ biến môi trường (xem netlify.toml / vercel.json). */
window.VF_SECRETS = { GEMINI_API_KEY: 'AIza...dán key Gemini của bạn vào đây rồi đổi tên file thành secrets.js' };
